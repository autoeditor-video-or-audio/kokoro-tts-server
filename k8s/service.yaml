apiVersion: v1
kind: Service
metadata:
  name: kokoro-tts-server
  labels:
    app: kokoro-tts-server
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: kokoro-tts-server
